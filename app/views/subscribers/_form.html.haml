= form_for(@subscriber) do |f|
  - if @subscriber.errors.any?
    #error_explanation
      %h2= "#{pluralize(@subscriber.errors.count, "error")} prohibited this subscriber from being saved:"
      %ul
        - @subscriber.errors.full_messages.each do |msg|
          %li= msg

  = hidden_field :filter, :laboratory, value: @filter_laboratory
  = hidden_field :filter, :condition, value: @filter_condition
  %div
    - if @subscriber.new_record?
      Create a subscriber for this filter:
    - else
      This is a subscriber for this filter:
    %ul
      %li Laboratory: #{@laboratory.try(:name)}
      %li Condition: #{@filter_condition}
  .control-group
    = f.label :name, :class => 'control-label'
    .control
      = f.text_field :name
  .control-group
    = f.label :url, "URL", :class => 'control-label'
    .control
      = f.text_field :url
  .control-group
    = f.label :url_user, "User (optional)", :class => 'control-label'
    .control
      = f.text_field :url_user
  .control-group
    = f.label :url_password, "Password (optional)", :class => 'control-label'
    .control
      = f.password_field :url_password
  .control-group
    = f.label :fields, :class => 'control-label'
    .control
      - ["patient_id", "patient_name", "patient_telephone_number", "patient_zip_code", "created_at", "device_uuid", "institution_id", "laboratory_id", "location_id", "assay_name", "device_serial_number", "age", "gender", "system_user", "start_time", "condition", "result"].each do |field|
        %label
          = check_box_tag "fields[#{field}]", true, @subscriber.fields.include?(field)
          = field
  .control-group
    .control
      %input.btn{type: "submit", value: "Create subscriber"}
