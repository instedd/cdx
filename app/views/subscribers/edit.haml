%h3 Editing #{subscriber.name}

= render 'form'

%hr

= icon_link_to :'icf-delete', "Delete this subscriber", [institution, subscriber], method: :delete, confirm: "Are you sure you want to delete the subscriber '#{subscriber.name}'"
