.span12
  %h3 Edit #{subscriber.name}

  = render 'form'

  %hr

  = icon_link_to :'icf-delete', "Delete this subscriber", subscriber, method: :delete, data: { confirm: "Are you sure you want to delete the location '#{subscriber.name}'" }
