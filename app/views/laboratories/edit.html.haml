%h2
  = link_to '←', laboratories_path
  = @laboratory.name

= render 'form'

- content_for(:rightcolumn) do
  - if @can_delete
    = link_to "Delete", [@institution, @laboratory], method: :delete, data: { confirm: "Are you sure you want to delete the laboratory '#{@laboratory.name}'" }, class: 'btn-primary fixed', title: 'Delete Laboratory'
  
