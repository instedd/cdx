%h1
  = link_to policies_path, class: 'side-link', title: 'Back' do
    = image_tag "arrow-left.png"
  Editing #{@policy.name}

= render 'form'


= link_to "Delete this policy", @policy, method: :delete, data: { confirm: "Are you sure you want to delete the policy '#{@policy.name}'" }, :class => 'btn-secondary pull-right'
