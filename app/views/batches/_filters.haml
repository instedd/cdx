- content_for(:subheader) do
  .row.center.filters
    .col.pe-10
      .row
        .col
          %h1
            = link_to "+", sample_or_batch_creation_batches_path, class: 'btn-add side-link fix', title: 'Add batch'
            Batches

      %form#filters-form{action: batches_path, "data-auto-submit" => true}
        %input{type: "hidden", name: "page_size", value: @page_size}
        .row
          .filter
            %label.block Isolate Name
            %input.input-x-large{type: "text", name: "isolate_name", value: params["isolate_name"], placeholder: "Filter by isolate name"}
