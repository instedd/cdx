- content_for(:subheader) do
  .row.center.add-new
    .col.pe-10
      .row
        .col
          %h2
            = link_to alerts_path, class: 'side-link', title: 'Back' do
              = image_tag "arrow-left-white.png"
            New Alert

= react_component('AlertCategorySelect', category_types:Alert.category_types, aa:"vvv", url:"/alerts.json", sites:@sites, anomalie_types:Alert.anomalie_types)

%br/
%br/
%hr/
%hr/		
= render 'form'
