- content_for(:subheader) do
  .row.center.edit
    .col.pe-10
      .row
        .col
          %h2
            = link_to test_results_path, class: 'side-link', title: 'Back' do
              = image_tag "arrow-left.png"
            Encounter

= react_component('EncounterForm', encounter: @encounter_as_json, context: @navigation_context.to_hash)
