.confusion-matrix 
  .confusion-matrix-row
    .confusion-matrix-subtitle.muted
      
    .confusion-matrix-subtitle.muted
      PREDICTED 
      %br
      NEGATIVE
    .confusion-matrix-subtitle.muted
      PREDICTED 
      %br
      POSITIVE
    .confusion-matrix-subtitle.muted
      TOTAL

  .confusion-matrix-row
    .confusion-matrix-subtitle.rotated.muted
      ACTUAL 
      %br
      NEGATIVE
    .confusion-matrix-cell.gray
      .number
        = confusion_matrix(@samples_report)[:true_negative]
      .legend 
        True Negative
    .confusion-matrix-cell.gray
      .number
        = confusion_matrix(@samples_report)[:false_positive]
      .legend 
        False Positive
    .confusion-matrix-cell.muted
      .number
        = confusion_matrix(@samples_report)[:actual_negative]
      .legend 
        Actual Negative

  .confusion-matrix-row
    .confusion-matrix-subtitle.rotated.muted
      ACTUAL 
      %br
      POSITIVE
    .confusion-matrix-cell.gray
      .number
        = confusion_matrix(@samples_report)[:false_negative]
      .legend 
        False Negative
    .confusion-matrix-cell.gray
      .number
        = confusion_matrix(@samples_report)[:true_positive]
      .legend 
        True Positive
    .confusion-matrix-cell.muted
      .number
        = confusion_matrix(@samples_report)[:actual_positive]
      .legend 
        Actual Positive

  .confusion-matrix-row
    .confusion-matrix-subtitle.rotated.muted
      TOTAL
    .confusion-matrix-cell.muted
      .number
        = confusion_matrix(@samples_report)[:predicted_negative]
      .legend 
        Predicted Negative
    .confusion-matrix-cell.muted
      .number
        = confusion_matrix(@samples_report)[:predicted_positive]
      .legend 
        Predicted Positive
    .confusion-matrix-cell.muted
      .number
        = confusion_matrix(@samples_report)[:total]
      .legend 
        Total