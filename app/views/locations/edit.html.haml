%h3 Editing #{@location.name}

= render 'form'

%hr

= icon_link_to :'icf-delete', "Delete this location", @location, method: :delete, confirm: "Are you sure you want to delete the location '#{@location.name}'"
